def fac(x):
        if x == 0 or x ==1:
                return 1
        else:
                mul=1
                while x>1:
                        mul=mul*(x)
                        x-=1
                return mul
print(fac(4))
